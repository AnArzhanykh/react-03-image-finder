// window.scrollTo({
//   top: document.documentElement.scrollHeight,
//   behavior: 'smooth',
// });

const Button =()=>{
    return(
        <button type='"submit'>Load more</button>
    )
}

export default Button;